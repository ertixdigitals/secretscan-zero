# secretscan-zero
SecretScan-Zero is a zero-backend, zero-knowledge secret scanner that detects and redacts hardcoded API keys, tokens, and credentials entirely in your browser. Running via a local Web Worker with zero server uploads, it delivers sanitized ZIPs, AI prompts, and audit reports safely.  
